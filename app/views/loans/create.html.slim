h1 Loans#create
p Find me in app/views/loans/create.html.slim

p
  h2 Users for matching ISBN
  = render "shared/loans_create", response: @response[:by_isbn]

p
  h2 Users for matching cards
  = render "shared/loans_create", response: @response[:by_card]

p
  h2 Users for matching names
  = render "shared/loans_create", response: @response[:by_name]

p
  h2 Raw data to debug
  == ap(@response, html: true)
